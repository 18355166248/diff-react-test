{
  "name": "diff-react-test",
  "version": "1.2.3",
  "description": "",
  "main": "index.js",
  "scripts": {
    "changelog": "conventional-changelog -n './changelog-custom' -i CHANGELOG.md -s -r 0",
    "version": "npm run changelog && git add CHANGELOG.md",
    "postversion": "git push & git push origin --tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "compare-func": "^2.0.0"
  }
}
